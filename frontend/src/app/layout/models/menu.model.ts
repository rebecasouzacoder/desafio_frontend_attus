/**
 * Representa a estrutura de um item de navegação
 * do menu lateral da aplicação.
 *
 * @export
 * @interface IMenu
 */
export interface IMenu {
    title: string;
    url: string;
    icon: string;
}